# railway.toml (at project root)
[build]
builder = "dockerfile"

[deploy]
startCommand = "python setup_db.py && uvicorn main:app --host 0.0.0.0 --port $PORT"
healthCheckPath = "/health"
healthCheckTimeout = 60